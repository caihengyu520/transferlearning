该论文采用GAN思想来训练自适应神经网络，而并非采用较为传统的MMD距离等度量方式前来学习。
网络框架有三部分组成，特征提取器，标签分类器，域分类器组成
为了实现分类能力，要求标签分类其和域分类器损失越小越好，这样模型区分能力变强，
林一方面，为了学习域不变适应能力，又要求域分类器损失越大越好
因此引入了梯度反转
在前向传播时对损失函数计算中用到了一个恒等映射函数，将其用于域损失，反向传播时，该恒等函数梯度会乘上一个负数因子，代表参数权值增加，代表增强域不变适应能力，该负数因子体现了域区分能力与域不变适应能力的折中。
